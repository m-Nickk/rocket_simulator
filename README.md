# Rocket Simulator (Alpha)

A simple rocket flight simulator written in Python using the Pygame library.

## How to Run

1. Install Pygame if you haven't already:
   ```bash
   pip install pygame
   ```

2. Run the simulator:
   ```bash
   python rocket_simulator.py
   ```

## Controls
* **Spacebar** — toggle the rocket engine on or off.
